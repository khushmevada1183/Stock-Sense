import React from 'react';
import AlertsNewsPageClient from './page-client';

export const metadata = {
  title: 'Market Alerts | Stock Sense',
  description: 'Important market alerts, price notifications, and critical financial updates',
};

export default function AlertsNewsPage() {
  return <AlertsNewsPageClient />;
} 